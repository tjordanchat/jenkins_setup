url=""
binding.variables.each {
   if ( "${it.key}" == "URL" ) {
      url="${it.value}"
   }
}

pipelineJob('Pipeline') {
  scm {
        git('git:'+url)
  }
  definition {
    cps {
      sandbox()
      script("def URL = '"+url+"'") 
    }
  }
}
