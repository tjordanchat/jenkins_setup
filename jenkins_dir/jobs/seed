pipelineJob('Pipeline') {
  definition {
    cps {
      sandbox()
      binding.variables.each {
         if ( "${it.key}" == "URL" ) {
         url="${it.value}"
      }
      script("def URL = '"+url+"'") 
    }
  }
}
