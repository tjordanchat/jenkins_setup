---
sudo: true
language: python

addons:
  apt:
    packages:
      - xvfb
      - figlet
      - toilet
  
script:
  - curl https://raw.githubusercontent.com/tjordanchat/jenkins_setup/master/install.sh | sh
  - echo Done
