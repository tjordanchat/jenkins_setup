set -x
export PDIR="$(puppet config print confdir)"
ls -la
OPWD=`pwd`
find puppet_dir -type f | while read file
do
	F=$(echo $file | sed "s#puppet_dir##")
	D=$(dirname $F)
	sudo mkdir -p $PDIR/$D
	sudo cp $file $PDIR/$F
done
cd $OPWD
