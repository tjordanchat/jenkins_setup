set -x
ls -la
OPWD=`pwd`
find puppet_dir -type f | while read file
do
	F=$(echo $file | sed "s#puppet_dir#$HOME/.puppet#")
	D=$(dirname $F)
	sudo mkdir -p /etc/puppet/$D
	sudo cp $file /etc/puppet/$F
done
cd $OPWD
